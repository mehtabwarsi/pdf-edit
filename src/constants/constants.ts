import { Dimensions } from "react-native";

const { height: SCREEN_HIGHT, width: SCREEN_WIDTH } = Dimensions.get('window');

const COLORS = {
    black: 'black',
    white: 'white',
    Red: 'red'
}

export {
    SCREEN_WIDTH,
    SCREEN_HIGHT,
    COLORS
}